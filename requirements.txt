# JOB.ai dependencies.
#
# Re-encoded as UTF-8: this file was previously UTF-16 (a PowerShell
# `pip freeze >` artefact), which made `pip install -r requirements.txt` fail
# outright on every platform.
#
# Removed with the external LLM providers: langchain-groq, langchain-openai,
# groq, openai, llamaapi, tiktoken, llama-cpp-python, chromadb + chroma-hnswlib,
# spacy/thinc/pyresparser, fastapi/flask/uvicorn/starlette and the
# opentelemetry / kubernetes / posthog trees they pulled in. None of them are
# imported any more.

# --- core ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic[email]
pydantic-extra-types==2.10.1
phonenumbers==8.13.50
requests==2.32.3
numpy==1.26.4
pandas==2.2.3

# --- retrieval ---
faiss-cpu==1.9.0
langchain-core==0.3.25
langchain-community==0.3.7
langchain-huggingface==0.1.2
sentence-transformers==3.3.1     # embeddings + the cross-encoder reranker
transformers==4.47.1
torch==2.5.1
rank-bm25==0.2.2                 # lexical retrieval arm

# --- storage ---
pymongo==4.10.1
neo4j==5.27.0                    # optional: falls back to an in-process graph

# --- ingestion ---
# Playwright drives every DOM-scraped source (Naukri today). After installing,
# fetch the browser:  playwright install chromium
playwright==1.49.1
beautifulsoup4==4.12.3
lxml==5.3.0
# Selenium is retained only for the older per-source scripts under
# Scrape_entire_web/ and cuvete/, which have not been migrated yet.
selenium==4.25.0

# --- interfaces ---
python-telegram-bot==21.7
PyMuPDF==1.25.1                  # resume PDF text extraction

# --- development ---
pytest==8.3.4

# The LLM is local Ollama, reached over plain HTTP with `requests`.
# No Python client is required; install the app from https://ollama.com
# and run:  ollama pull qwen2.5:7b
